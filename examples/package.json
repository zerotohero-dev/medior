{
  "name": "examples",
  "private": "true",
  "version": "1.0.0",
  "description": "An example package.json so that you can install `medior` inside this folder.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Volkan Özçelik",
  "license": "MIT",
  "dependencies": {
    "medior": "*"
  }
}
